{"input": "Write a Go function 'Fibonacci(n int) int' that returns the nth number in the Fibonacci sequence. Assume n=0 is 0 and n=1 is 1.", "output": "Placeholder for RL training", "extra_env_info": {"test_code": "package main\nimport \"testing\"\nfunc TestFib(t *testing.T) {\n cases := []struct{n, want int}{{0,0},{1,1},{5,5},{10,55}}\n for _, c := range cases {\n if got := Fibonacci(c.n); got != c.want {\n t.Errorf(\"Fib(%d) = %d; want %d\", c.n, got, c.want)\n }\n }\n}"}}
{"input": "Write a Go function 'IsPalindrome(s string) bool' that returns true if a string is a palindrome, ignoring case.", "output": "Placeholder for RL training", "extra_env_info": {"test_code": "package main\nimport \"testing\"\nimport \"strings\"\nfunc TestPalindrome(t *testing.T) {\n cases := []struct{s string; want bool}{{\"racecar\",true},{\"Hello\",false},{\"Madam\",true}}\n for _, c := range cases {\n if got := IsPalindrome(c.s); got != c.want {\n t.Errorf(\"IsPalindrome(%q) = %v; want %v\", c.s, got, c.want)\n }\n }\n}"}}
{"input": "Write a Go function 'Factorial(n int) int'. Return 1 for n=0.", "output": "Placeholder for RL training", "extra_env_info": {"test_code": "package main\nimport \"testing\"\nfunc TestFactorial(t *testing.T) {\n cases := []struct{n, want int}{{0,1},{3,6},{5,120}}\n for _, c := range cases {\n if got := Factorial(c.n); got != c.want {\n t.Errorf(\"Factorial(%d) = %d; want %d\", c.n, got, c.want)\n }\n }\n}"}}
{"input": "Write a Go function 'SumArray(numbers []int) int' that returns the sum of all elements in a slice.", "output": "Placeholder for RL training", "extra_env_info": {"test_code": "package main\nimport \"testing\"\nfunc TestSum(t *testing.T) {\n cases := []struct{in []int; want int}{{[]int{1,2,3},6},{[]int{-1,1},0},{[]int{},0}}\n for _, c := range cases {\n if got := SumArray(c.in); got != c.want {\n t.Errorf(\"Sum(%v) = %d; want %d\", c.in, got, c.want)\n }\n }\n}"}}